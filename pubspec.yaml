name: flutter_getx_testing
description: A new Flutter project covered with Unit, Widget & Integration tests.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  speed_up: ^0.4.0
  speed_up_flutter: ^0.4.3
  speed_up_get: ^0.0.1
  get: ^4.3.8
  rxdart: ^0.27.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter  
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true